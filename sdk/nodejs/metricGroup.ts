// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as enums from "./types/enums";
import * as utilities from "./utilities";

/**
 * Provides a LaunchDarkly metric group resource.
 *
 * > **Beta:** This resource uses a beta LaunchDarkly API. Beta resources may change or be removed in future versions.
 *
 * This resource allows you to create and manage metric groups within your LaunchDarkly project. A metric group is an ordered `funnel` or an unordered `standard` collection of metrics that you can reference from experiments. To learn more, read [Experimentation Documentation](https://launchdarkly.com/docs/home/experimentation).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as launchdarkly from "@pulumi/launchdarkly";
 *
 * const checkoutFunnel = new launchdarkly.MetricGroup("checkout_funnel", {
 *     projectKey: example.key,
 *     key: "checkout-funnel",
 *     name: "Checkout funnel",
 *     kind: "funnel",
 *     description: "Ordered funnel of the steps a customer takes through checkout",
 *     metrics: [
 *         {
 *             key: viewedCart.key,
 *             name_in_group: "Viewed cart",
 *         },
 *         {
 *             key: startedCheckout.key,
 *             name_in_group: "Started checkout",
 *         },
 *         {
 *             key: completedPurchase.key,
 *             name_in_group: "Completed purchase",
 *         },
 *     ],
 *     tags: [
 *         "checkout",
 *         "experimentation",
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * A metric group can be imported using the project key and metric group key
 * joined by a slash: project_key/metric_group_key.
 *
 * ```sh
 * $ pulumi import launchdarkly:index/metricGroup:MetricGroup checkout_funnel example-project/checkout-funnel
 * ```
 */
export class MetricGroup extends pulumi.CustomResource {
    /**
     * Get an existing MetricGroup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MetricGroupState, opts?: pulumi.CustomResourceOptions): MetricGroup {
        return new MetricGroup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'launchdarkly:index/metricGroup:MetricGroup';

    /**
     * Returns true if the given object is an instance of MetricGroup.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is MetricGroup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === MetricGroup.__pulumiType;
    }

    /**
     * A description of the metric group's purpose.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * The unique key that references the metric group. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly key: pulumi.Output<string>;
    /**
     * The type of the metric group. Available choices are `funnel` and `standard`. A `funnel` metric group is an ordered list of metrics. A `standard` metric group is an unordered collection. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly kind: pulumi.Output<string>;
    /**
     * The LaunchDarkly member ID of the member who maintains the metric group. If not set when the metric group is created, the provider assigns the member associated with the access token. Service tokens have no associated member, so configurations using one must set this explicitly.
     */
    declare public readonly maintainerId: pulumi.Output<string>;
    /**
     * An ordered list of the metrics in this metric group. Must contain at least two metrics. For `funnel` metric groups the order is significant and each metric requires a `nameInGroup`.
     */
    declare public readonly metrics: pulumi.Output<outputs.MetricGroupMetric[]>;
    /**
     * The human-friendly name for the metric group.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The metric group's project key. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly projectKey: pulumi.Output<string>;
    /**
     * Tags associated with the metric group.
     */
    declare public readonly tags: pulumi.Output<string[] | undefined>;
    /**
     * The version of the metric group.
     */
    declare public /*out*/ readonly version: pulumi.Output<number>;

    /**
     * Create a MetricGroup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: MetricGroupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MetricGroupArgs | MetricGroupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MetricGroupState | undefined;
            resourceInputs["description"] = state?.description;
            resourceInputs["key"] = state?.key;
            resourceInputs["kind"] = state?.kind;
            resourceInputs["maintainerId"] = state?.maintainerId;
            resourceInputs["metrics"] = state?.metrics;
            resourceInputs["name"] = state?.name;
            resourceInputs["projectKey"] = state?.projectKey;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["version"] = state?.version;
        } else {
            const args = argsOrState as MetricGroupArgs | undefined;
            if (args?.key === undefined && !opts.urn) {
                throw new Error("Missing required property 'key'");
            }
            if (args?.kind === undefined && !opts.urn) {
                throw new Error("Missing required property 'kind'");
            }
            if (args?.metrics === undefined && !opts.urn) {
                throw new Error("Missing required property 'metrics'");
            }
            if (args?.projectKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'projectKey'");
            }
            resourceInputs["description"] = args?.description;
            resourceInputs["key"] = args?.key;
            resourceInputs["kind"] = args?.kind;
            resourceInputs["maintainerId"] = args?.maintainerId;
            resourceInputs["metrics"] = args?.metrics;
            resourceInputs["name"] = args?.name;
            resourceInputs["projectKey"] = args?.projectKey;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["version"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(MetricGroup.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering MetricGroup resources.
 */
export interface MetricGroupState {
    /**
     * A description of the metric group's purpose.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The unique key that references the metric group. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    key?: pulumi.Input<string | undefined>;
    /**
     * The type of the metric group. Available choices are `funnel` and `standard`. A `funnel` metric group is an ordered list of metrics. A `standard` metric group is an unordered collection. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    kind?: pulumi.Input<string | undefined>;
    /**
     * The LaunchDarkly member ID of the member who maintains the metric group. If not set when the metric group is created, the provider assigns the member associated with the access token. Service tokens have no associated member, so configurations using one must set this explicitly.
     */
    maintainerId?: pulumi.Input<string | undefined>;
    /**
     * An ordered list of the metrics in this metric group. Must contain at least two metrics. For `funnel` metric groups the order is significant and each metric requires a `nameInGroup`.
     */
    metrics?: pulumi.Input<pulumi.Input<inputs.MetricGroupMetric>[] | undefined>;
    /**
     * The human-friendly name for the metric group.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The metric group's project key. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    projectKey?: pulumi.Input<string | undefined>;
    /**
     * Tags associated with the metric group.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The version of the metric group.
     */
    version?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a MetricGroup resource.
 */
export interface MetricGroupArgs {
    /**
     * A description of the metric group's purpose.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The unique key that references the metric group. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    key: pulumi.Input<string>;
    /**
     * The type of the metric group. Available choices are `funnel` and `standard`. A `funnel` metric group is an ordered list of metrics. A `standard` metric group is an unordered collection. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    kind: pulumi.Input<string>;
    /**
     * The LaunchDarkly member ID of the member who maintains the metric group. If not set when the metric group is created, the provider assigns the member associated with the access token. Service tokens have no associated member, so configurations using one must set this explicitly.
     */
    maintainerId?: pulumi.Input<string | undefined>;
    /**
     * An ordered list of the metrics in this metric group. Must contain at least two metrics. For `funnel` metric groups the order is significant and each metric requires a `nameInGroup`.
     */
    metrics: pulumi.Input<pulumi.Input<inputs.MetricGroupMetric>[]>;
    /**
     * The human-friendly name for the metric group.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The metric group's project key. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    projectKey: pulumi.Input<string>;
    /**
     * Tags associated with the metric group.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

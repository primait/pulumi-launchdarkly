// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as enums from "./types/enums";
import * as utilities from "./utilities";

/**
 * Provides a LaunchDarkly release policy resource.
 *
 * > **Beta:** This resource wraps a beta LaunchDarkly API (the `release-policies` endpoints, accessed with the `LD-API-Version: beta` header). Beta resources may change or be removed in future versions.
 *
 * This resource lets you create and manage [release policies](https://launchdarkly.com/docs/home/releases/release-policies) within a LaunchDarkly project. A release policy defines how flag changes roll out to environments, either as a `guarded-release`, which monitors the rollout against metrics with optional automatic rollback, or a `progressive-release`, which advances the rollout through a fixed schedule of allocation stages.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as launchdarkly from "@pulumi/launchdarkly";
 *
 * const guardedCheckout = new launchdarkly.ReleasePolicy("guarded_checkout", {
 *     projectKey: example.key,
 *     key: "guarded-checkout",
 *     name: "Guarded checkout rollout",
 *     releaseMethod: "guarded-release",
 *     scope: {
 *         environment_keys: ["production"],
 *         flag_tag_keys: ["checkout"],
 *     },
 *     guardedReleaseConfig: {
 *         rollout_context_kind: "user",
 *         min_sample_size: 1000,
 *         rollback_on_regression: true,
 *         metric_keys: [completedPurchase.key],
 *         stages: [
 *             {
 *                 allocation: 10,
 *                 durationMillis: 3600000,
 *             },
 *             {
 *                 allocation: 50,
 *                 durationMillis: 3600000,
 *             },
 *         ],
 *     },
 * });
 * const progressiveRollout = new launchdarkly.ReleasePolicy("progressive_rollout", {
 *     projectKey: example.key,
 *     key: "progressive-rollout",
 *     name: "Progressive rollout",
 *     releaseMethod: "progressive-release",
 *     scope: {
 *         environment_keys: ["production"],
 *     },
 *     progressiveReleaseConfig: {
 *         rollout_context_kind: "user",
 *         stages: [
 *             {
 *                 allocation: 20,
 *                 durationMillis: 3600000,
 *             },
 *             {
 *                 allocation: 60,
 *                 durationMillis: 3600000,
 *             },
 *         ],
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * A release policy can be imported using the project key and release policy key
 * joined by a slash: project_key/release_policy_key.
 *
 * ```sh
 * $ pulumi import launchdarkly:index/releasePolicy:ReleasePolicy guarded_checkout example-project/guarded-checkout
 * ```
 */
export class ReleasePolicy extends pulumi.CustomResource {
    /**
     * Get an existing ReleasePolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ReleasePolicyState, opts?: pulumi.CustomResourceOptions): ReleasePolicy {
        return new ReleasePolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'launchdarkly:index/releasePolicy:ReleasePolicy';

    /**
     * Returns true if the given object is an instance of ReleasePolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ReleasePolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ReleasePolicy.__pulumiType;
    }

    /**
     * Configuration for a `guarded-release`. May only be set when `releaseMethod` is `guarded-release`.
     */
    declare public readonly guardedReleaseConfig: pulumi.Output<outputs.ReleasePolicyGuardedReleaseConfig | undefined>;
    /**
     * The unique human-readable key that references the release policy. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly key: pulumi.Output<string>;
    /**
     * The human-friendly name for the release policy.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Configuration for a `progressive-release`. May only be set when `releaseMethod` is `progressive-release`.
     */
    declare public readonly progressiveReleaseConfig: pulumi.Output<outputs.ReleasePolicyProgressiveReleaseConfig | undefined>;
    /**
     * The release policy's project key. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly projectKey: pulumi.Output<string>;
    /**
     * The rank of the release policy within the project. LaunchDarkly assigns and orders the rank. Reorder release policies in the LaunchDarkly UI.
     */
    declare public /*out*/ readonly rank: pulumi.Output<number>;
    /**
     * The release method this policy uses. Must be one of `guarded-release` or `progressive-release`. Set `guardedReleaseConfig` for a `guarded-release` and `progressiveReleaseConfig` for a `progressive-release`.
     */
    declare public readonly releaseMethod: pulumi.Output<string>;
    /**
     * The scope that determines which environments and flags this release policy applies to.
     */
    declare public readonly scope: pulumi.Output<outputs.ReleasePolicyScope | undefined>;

    /**
     * Create a ReleasePolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ReleasePolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ReleasePolicyArgs | ReleasePolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ReleasePolicyState | undefined;
            resourceInputs["guardedReleaseConfig"] = state?.guardedReleaseConfig;
            resourceInputs["key"] = state?.key;
            resourceInputs["name"] = state?.name;
            resourceInputs["progressiveReleaseConfig"] = state?.progressiveReleaseConfig;
            resourceInputs["projectKey"] = state?.projectKey;
            resourceInputs["rank"] = state?.rank;
            resourceInputs["releaseMethod"] = state?.releaseMethod;
            resourceInputs["scope"] = state?.scope;
        } else {
            const args = argsOrState as ReleasePolicyArgs | undefined;
            if (args?.key === undefined && !opts.urn) {
                throw new Error("Missing required property 'key'");
            }
            if (args?.projectKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'projectKey'");
            }
            if (args?.releaseMethod === undefined && !opts.urn) {
                throw new Error("Missing required property 'releaseMethod'");
            }
            resourceInputs["guardedReleaseConfig"] = args?.guardedReleaseConfig;
            resourceInputs["key"] = args?.key;
            resourceInputs["name"] = args?.name;
            resourceInputs["progressiveReleaseConfig"] = args?.progressiveReleaseConfig;
            resourceInputs["projectKey"] = args?.projectKey;
            resourceInputs["releaseMethod"] = args?.releaseMethod;
            resourceInputs["scope"] = args?.scope;
            resourceInputs["rank"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ReleasePolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ReleasePolicy resources.
 */
export interface ReleasePolicyState {
    /**
     * Configuration for a `guarded-release`. May only be set when `releaseMethod` is `guarded-release`.
     */
    guardedReleaseConfig?: pulumi.Input<inputs.ReleasePolicyGuardedReleaseConfig | undefined>;
    /**
     * The unique human-readable key that references the release policy. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    key?: pulumi.Input<string | undefined>;
    /**
     * The human-friendly name for the release policy.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Configuration for a `progressive-release`. May only be set when `releaseMethod` is `progressive-release`.
     */
    progressiveReleaseConfig?: pulumi.Input<inputs.ReleasePolicyProgressiveReleaseConfig | undefined>;
    /**
     * The release policy's project key. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    projectKey?: pulumi.Input<string | undefined>;
    /**
     * The rank of the release policy within the project. LaunchDarkly assigns and orders the rank. Reorder release policies in the LaunchDarkly UI.
     */
    rank?: pulumi.Input<number | undefined>;
    /**
     * The release method this policy uses. Must be one of `guarded-release` or `progressive-release`. Set `guardedReleaseConfig` for a `guarded-release` and `progressiveReleaseConfig` for a `progressive-release`.
     */
    releaseMethod?: pulumi.Input<string | undefined>;
    /**
     * The scope that determines which environments and flags this release policy applies to.
     */
    scope?: pulumi.Input<inputs.ReleasePolicyScope | undefined>;
}

/**
 * The set of arguments for constructing a ReleasePolicy resource.
 */
export interface ReleasePolicyArgs {
    /**
     * Configuration for a `guarded-release`. May only be set when `releaseMethod` is `guarded-release`.
     */
    guardedReleaseConfig?: pulumi.Input<inputs.ReleasePolicyGuardedReleaseConfig | undefined>;
    /**
     * The unique human-readable key that references the release policy. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    key: pulumi.Input<string>;
    /**
     * The human-friendly name for the release policy.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Configuration for a `progressive-release`. May only be set when `releaseMethod` is `progressive-release`.
     */
    progressiveReleaseConfig?: pulumi.Input<inputs.ReleasePolicyProgressiveReleaseConfig | undefined>;
    /**
     * The release policy's project key. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    projectKey: pulumi.Input<string>;
    /**
     * The release method this policy uses. Must be one of `guarded-release` or `progressive-release`. Set `guardedReleaseConfig` for a `guarded-release` and `progressiveReleaseConfig` for a `progressive-release`.
     */
    releaseMethod: pulumi.Input<string>;
    /**
     * The scope that determines which environments and flags this release policy applies to.
     */
    scope?: pulumi.Input<inputs.ReleasePolicyScope | undefined>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a LaunchDarkly big segment store (persistent store) integration resource.
 *
 * > **Beta:** This resource wraps a beta LaunchDarkly API. Beta resources may change or be removed in future provider versions.
 *
 * This resource lets you create and manage a persistent store integration for an environment. Server-side SDKs use a persistent store, backed by Redis or DynamoDB in your own infrastructure, to evaluate segments synced from external tools and larger list-based segments. To learn more, read [Segment configuration](https://launchdarkly.com/docs/home/flags/segment-config).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as launchdarkly from "@pulumi/launchdarkly";
 *
 * const redisStore = new launchdarkly.BigSegmentStoreIntegration("redis_store", {
 *     projectKey: example.key,
 *     environmentKey: "production",
 *     integrationKey: "redis",
 *     name: "Production Redis persistent store",
 *     on: true,
 *     config: JSON.stringify({
 *         host: "redis.internal.example.com",
 *         port: "6379",
 *         tlsEnabled: true,
 *         username: "default",
 *         password: "example-password",
 *     }),
 *     tags: ["terraform-managed"],
 * });
 * const dynamodbStore = new launchdarkly.BigSegmentStoreIntegration("dynamodb_store", {
 *     projectKey: example.key,
 *     environmentKey: "production",
 *     integrationKey: "dynamodb",
 *     name: "Production DynamoDB persistent store",
 *     on: true,
 *     config: JSON.stringify({
 *         tableName: "launchdarkly-big-segments",
 *         region: "us-east-1",
 *         roleArn: "arn:aws:iam::123456789012:role/launchdarkly-big-segments",
 *         externalId: "00000000-0000-0000-0000-000000000000",
 *     }),
 *     tags: ["terraform-managed"],
 * });
 * ```
 *
 * ## Import
 *
 * A big segment store integration can be imported using the project key,
 * environment key, integration key, and server-assigned integration ID joined by
 * slashes: project_key/environment_key/integration_key/integration_id.
 *
 * ```sh
 * $ pulumi import launchdarkly:index/bigSegmentStoreIntegration:BigSegmentStoreIntegration redis_store example-project/production/redis/57c0a8e4f7b9c20b3c5d1234
 * ```
 */
export class BigSegmentStoreIntegration extends pulumi.CustomResource {
    /**
     * Get an existing BigSegmentStoreIntegration resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BigSegmentStoreIntegrationState, opts?: pulumi.CustomResourceOptions): BigSegmentStoreIntegration {
        return new BigSegmentStoreIntegration(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'launchdarkly:index/bigSegmentStoreIntegration:BigSegmentStoreIntegration';

    /**
     * Returns true if the given object is an instance of BigSegmentStoreIntegration.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BigSegmentStoreIntegration {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BigSegmentStoreIntegration.__pulumiType;
    }

    /**
     * A JSON string holding the store-specific configuration. All values are strings except `tlsEnabled`, which is a boolean. For `redis`, the fields are `host`, `port`, `tlsEnabled`, `username`, and `password`. For `dynamodb`, they are `tableName`, `region`, `roleArn`, and `externalId`, a UUID. Marked sensitive because it carries credentials. The API redacts secrets and normalizes this value on read, so it is treated as write-only: Terraform stores and diffs the value you provide and does not reconcile it against the server, so configuration changes made outside Terraform are not detected as drift.
     */
    declare public readonly config: pulumi.Output<string>;
    /**
     * The key of the environment the integration belongs to. Persistent store integrations are environment-scoped. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly environmentKey: pulumi.Output<string>;
    /**
     * The server-assigned ID of the integration configuration.
     */
    declare public /*out*/ readonly integrationId: pulumi.Output<string>;
    /**
     * The persistent store technology to use. Must be one of `redis` or `dynamodb`. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly integrationKey: pulumi.Output<string>;
    /**
     * A human-friendly name for the integration configuration.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Whether the integration is turned on. Defaults to `false`.
     */
    declare public readonly on: pulumi.Output<boolean>;
    /**
     * The key of the project the integration belongs to. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly projectKey: pulumi.Output<string>;
    /**
     * Tags associated with the integration configuration.
     */
    declare public readonly tags: pulumi.Output<string[] | undefined>;
    /**
     * The version of the integration configuration.
     */
    declare public /*out*/ readonly version: pulumi.Output<number>;

    /**
     * Create a BigSegmentStoreIntegration resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BigSegmentStoreIntegrationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BigSegmentStoreIntegrationArgs | BigSegmentStoreIntegrationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BigSegmentStoreIntegrationState | undefined;
            resourceInputs["config"] = state?.config;
            resourceInputs["environmentKey"] = state?.environmentKey;
            resourceInputs["integrationId"] = state?.integrationId;
            resourceInputs["integrationKey"] = state?.integrationKey;
            resourceInputs["name"] = state?.name;
            resourceInputs["on"] = state?.on;
            resourceInputs["projectKey"] = state?.projectKey;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["version"] = state?.version;
        } else {
            const args = argsOrState as BigSegmentStoreIntegrationArgs | undefined;
            if (args?.config === undefined && !opts.urn) {
                throw new Error("Missing required property 'config'");
            }
            if (args?.environmentKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'environmentKey'");
            }
            if (args?.integrationKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'integrationKey'");
            }
            if (args?.projectKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'projectKey'");
            }
            resourceInputs["config"] = args?.config ? pulumi.secret(args.config) : undefined;
            resourceInputs["environmentKey"] = args?.environmentKey;
            resourceInputs["integrationKey"] = args?.integrationKey;
            resourceInputs["name"] = args?.name;
            resourceInputs["on"] = args?.on;
            resourceInputs["projectKey"] = args?.projectKey;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["integrationId"] = undefined /*out*/;
            resourceInputs["version"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["config"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(BigSegmentStoreIntegration.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering BigSegmentStoreIntegration resources.
 */
export interface BigSegmentStoreIntegrationState {
    /**
     * A JSON string holding the store-specific configuration. All values are strings except `tlsEnabled`, which is a boolean. For `redis`, the fields are `host`, `port`, `tlsEnabled`, `username`, and `password`. For `dynamodb`, they are `tableName`, `region`, `roleArn`, and `externalId`, a UUID. Marked sensitive because it carries credentials. The API redacts secrets and normalizes this value on read, so it is treated as write-only: Terraform stores and diffs the value you provide and does not reconcile it against the server, so configuration changes made outside Terraform are not detected as drift.
     */
    config?: pulumi.Input<string | undefined>;
    /**
     * The key of the environment the integration belongs to. Persistent store integrations are environment-scoped. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    environmentKey?: pulumi.Input<string | undefined>;
    /**
     * The server-assigned ID of the integration configuration.
     */
    integrationId?: pulumi.Input<string | undefined>;
    /**
     * The persistent store technology to use. Must be one of `redis` or `dynamodb`. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    integrationKey?: pulumi.Input<string | undefined>;
    /**
     * A human-friendly name for the integration configuration.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Whether the integration is turned on. Defaults to `false`.
     */
    on?: pulumi.Input<boolean | undefined>;
    /**
     * The key of the project the integration belongs to. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    projectKey?: pulumi.Input<string | undefined>;
    /**
     * Tags associated with the integration configuration.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The version of the integration configuration.
     */
    version?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a BigSegmentStoreIntegration resource.
 */
export interface BigSegmentStoreIntegrationArgs {
    /**
     * A JSON string holding the store-specific configuration. All values are strings except `tlsEnabled`, which is a boolean. For `redis`, the fields are `host`, `port`, `tlsEnabled`, `username`, and `password`. For `dynamodb`, they are `tableName`, `region`, `roleArn`, and `externalId`, a UUID. Marked sensitive because it carries credentials. The API redacts secrets and normalizes this value on read, so it is treated as write-only: Terraform stores and diffs the value you provide and does not reconcile it against the server, so configuration changes made outside Terraform are not detected as drift.
     */
    config: pulumi.Input<string>;
    /**
     * The key of the environment the integration belongs to. Persistent store integrations are environment-scoped. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    environmentKey: pulumi.Input<string>;
    /**
     * The persistent store technology to use. Must be one of `redis` or `dynamodb`. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    integrationKey: pulumi.Input<string>;
    /**
     * A human-friendly name for the integration configuration.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Whether the integration is turned on. Defaults to `false`.
     */
    on?: pulumi.Input<boolean | undefined>;
    /**
     * The key of the project the integration belongs to. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    projectKey: pulumi.Input<string>;
    /**
     * Tags associated with the integration configuration.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a LaunchDarkly flag import configuration resource.
 *
 * > **Beta:** This resource wraps a beta LaunchDarkly API. Beta resources may change or be removed in future versions.
 *
 * This resource lets you create and manage flag import configurations, which import feature flags from an external feature management system (identified by `integrationKey`, for example `split`) into a LaunchDarkly project. The shape of `config` varies by integration and is described by the `formVariables` in that integration's manifest. To learn more, read [Importing flags from another provider](https://launchdarkly.com/docs/home/flags/import).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as launchdarkly from "@pulumi/launchdarkly";
 *
 * const splitImport = new launchdarkly.FlagImportConfiguration("split_import", {
 *     projectKey: example.key,
 *     integrationKey: "split",
 *     name: "Split flag import",
 *     config: JSON.stringify({
 *         workspaceApiKey: splitWorkspaceApiKey,
 *         workspaceId: splitWorkspaceId,
 *         environmentId: splitEnvironmentId,
 *         ldApiKey: launchdarklyApiKey,
 *     }),
 *     tags: [
 *         "imported",
 *         "split",
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * A flag import configuration can be imported using the composite ID in the format
 * `project_key/integration_key/integration_id`.
 *
 * ```sh
 * $ pulumi import launchdarkly:index/flagImportConfiguration:FlagImportConfiguration split_import example-project/split/57c12345abcd1234ef567890
 * ```
 */
export class FlagImportConfiguration extends pulumi.CustomResource {
    /**
     * Get an existing FlagImportConfiguration resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FlagImportConfigurationState, opts?: pulumi.CustomResourceOptions): FlagImportConfiguration {
        return new FlagImportConfiguration(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'launchdarkly:index/flagImportConfiguration:FlagImportConfiguration';

    /**
     * Returns true if the given object is an instance of FlagImportConfiguration.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FlagImportConfiguration {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FlagImportConfiguration.__pulumiType;
    }

    /**
     * A JSON-encoded object of configuration values for the integration. The accepted keys vary by `integrationKey` and are described by the `formVariables` in the integration's manifest (often including a secret API token). Marked sensitive because it commonly contains credentials.
     */
    declare public readonly config: pulumi.Output<string>;
    /**
     * The unique identifier the LaunchDarkly API assigns to this flag import configuration.
     */
    declare public /*out*/ readonly integrationId: pulumi.Output<string>;
    /**
     * The integration key identifying the external feature management system to import flags from, for example `split`. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly integrationKey: pulumi.Output<string>;
    /**
     * A human-friendly name for the flag import configuration. If not set, the LaunchDarkly API assigns a default.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The key of the project to import flags into. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    declare public readonly projectKey: pulumi.Output<string>;
    /**
     * Tags associated with the flag import configuration.
     */
    declare public readonly tags: pulumi.Output<string[] | undefined>;
    /**
     * The version of the flag import configuration.
     */
    declare public /*out*/ readonly version: pulumi.Output<number>;

    /**
     * Create a FlagImportConfiguration resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FlagImportConfigurationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FlagImportConfigurationArgs | FlagImportConfigurationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FlagImportConfigurationState | undefined;
            resourceInputs["config"] = state?.config;
            resourceInputs["integrationId"] = state?.integrationId;
            resourceInputs["integrationKey"] = state?.integrationKey;
            resourceInputs["name"] = state?.name;
            resourceInputs["projectKey"] = state?.projectKey;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["version"] = state?.version;
        } else {
            const args = argsOrState as FlagImportConfigurationArgs | undefined;
            if (args?.config === undefined && !opts.urn) {
                throw new Error("Missing required property 'config'");
            }
            if (args?.integrationKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'integrationKey'");
            }
            if (args?.projectKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'projectKey'");
            }
            resourceInputs["config"] = args?.config ? pulumi.secret(args.config) : undefined;
            resourceInputs["integrationKey"] = args?.integrationKey;
            resourceInputs["name"] = args?.name;
            resourceInputs["projectKey"] = args?.projectKey;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["integrationId"] = undefined /*out*/;
            resourceInputs["version"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["config"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(FlagImportConfiguration.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FlagImportConfiguration resources.
 */
export interface FlagImportConfigurationState {
    /**
     * A JSON-encoded object of configuration values for the integration. The accepted keys vary by `integrationKey` and are described by the `formVariables` in the integration's manifest (often including a secret API token). Marked sensitive because it commonly contains credentials.
     */
    config?: pulumi.Input<string | undefined>;
    /**
     * The unique identifier the LaunchDarkly API assigns to this flag import configuration.
     */
    integrationId?: pulumi.Input<string | undefined>;
    /**
     * The integration key identifying the external feature management system to import flags from, for example `split`. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    integrationKey?: pulumi.Input<string | undefined>;
    /**
     * A human-friendly name for the flag import configuration. If not set, the LaunchDarkly API assigns a default.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The key of the project to import flags into. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    projectKey?: pulumi.Input<string | undefined>;
    /**
     * Tags associated with the flag import configuration.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The version of the flag import configuration.
     */
    version?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a FlagImportConfiguration resource.
 */
export interface FlagImportConfigurationArgs {
    /**
     * A JSON-encoded object of configuration values for the integration. The accepted keys vary by `integrationKey` and are described by the `formVariables` in the integration's manifest (often including a secret API token). Marked sensitive because it commonly contains credentials.
     */
    config: pulumi.Input<string>;
    /**
     * The integration key identifying the external feature management system to import flags from, for example `split`. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    integrationKey: pulumi.Input<string>;
    /**
     * A human-friendly name for the flag import configuration. If not set, the LaunchDarkly API assigns a default.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The key of the project to import flags into. A change in this field forces the destruction of the existing resource and the creation of a new one.
     */
    projectKey: pulumi.Input<string>;
    /**
     * Tags associated with the flag import configuration.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}
